#include<bits/stdc++.h>
using namespace std;
// @Aayush2000
#define int         long long
#define intmax      LLONG_MAX
#define intmin      LLONG_MIN
#define flag        flag=0
#define vi          vector<int>
#define vs          vector<string>
#define pb          push_back
#define iarr(a)     for(int i=0;i<(sof(a)/sof(a[0]));i++)cin>>a[i];
#define oarr(a)     for(int i=0;i<(sof(a)/sof(a[0]));i++)cout<<a[i]<<" ";
#define mp          make_pair
#define endl        "\n"
#define sof         sizeof
#define dec         greater<int>();
#define endlr       endl;return;
#define debug(x)    cout<<#x<<" = "<<x<<endl;
#define debuga(x)   cout<<#x<<" = ";for(int i=0;i<(sof(x)/sof(x[0]));i++)cout<<x[i]<<" ";
#define maps        map<int,int>
#define mapss       map<string,int>
#define F first
#define S second
#define set0(x)     for(int i=0;i<(sof(x)/sof(x[0]));i++)x[i]=0;
#define foor(x,n)   for(int i=x;i<n;i++)
#define roof(x,n)   for(int i=x;i>=n;i--)
#define GT          "case #"
void solve();
int testcase=10;
// main 
int32_t main()
{ 
 if(testcase==1) {   solve();   return 0;}
 int t;cin>>t;
 while(t--) solve();
}

// solve 
void solve()
{
  


}